{{ system_prompt }}

The user asked: "{{ query }}"

This question is ambiguous and needs clarification before you can give a useful answer.
Rephrase the following clarifying question in your own natural voice. Vary the wording — do NOT use a fixed phrase:

Seed question: {{ clarifying_question }}

Ask the clarifying question conversationally, as if you are having a dialogue. Do not answer the question — only ask for clarification.
